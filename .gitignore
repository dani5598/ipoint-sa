*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/pail
/database/*.sqlite
/database/*.sqlite-journal
/ipointapple-hostinger.zip
.env.hostinger

# NOTE: vendor/ and public/build/ are intentionally COMMITTED so Hostinger's
# Git tool can deploy a runnable app without composer/npm (no SSH). Rebuild
# public/build with `npm run build` after front-end changes and re-commit.
Homestead.json
Homestead.yaml
Thumbs.db
